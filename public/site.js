const grid=document.querySelector('#members-grid'),status=document.querySelector('#status'),search=document.querySelector('#search');let members=[];const fallback='/logo.svg';
function esc(value){return String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function render(){const q=(search.value||'').trim().toLowerCase();const list=members.filter(m=>`${m.name} ${m.username}`.toLowerCase().includes(q));grid.innerHTML=list.length?list.map(m=>`<article class="card"><img src="${esc(m.avatar||fallback)}" onerror="this.src='${fallback}'" alt=""><div><h3>${esc(m.name)}</h3><p>${esc(m.username||'')}</p><div class="roles">${(m.roles||[]).map(r=>`<span class="role">${esc(r.name)}</span>`).join('')}</div></div></article>`).join(''):'<p>لا يوجد أعضاء بهذه الرتب.</p>'}
async function load(){try{const r=await fetch('/api/public/members');if(!r.ok)throw Error();const data=await r.json();members=data.members||[];status.textContent=`تم تحميل ${members.length} عضو`;status.className='status ok';render()}catch(e){status.textContent='تعذر الاتصال بالبوت حالياً';status.className='status'}}search.addEventListener('input',render);document.querySelector('#year').textContent=new Date().getFullYear();load();
