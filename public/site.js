const grid=document.querySelector('#members-grid');const status=document.querySelector('#status');const search=document.querySelector('#search');let members=[];const fallback='/logo.svg';
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function render(){const q=(search.value||'').trim().toLowerCase();const list=members.filter(m=>`${m.name} ${m.username} ${m.globalName||''}`.toLowerCase().includes(q));grid.innerHTML=list.length?list.map(m=>`<article class="card"><img src="${esc(m.avatar||fallback)}" onerror="this.src='${fallback}'" alt=""><div><h3>${esc(m.name)}</h3><p>@${esc((m.username||'').replace(/^@/,''))}</p><div class="roles">${(m.roles||[]).map(r=>`<span class="role">${esc(r.name)}</span>`).join('')}</div></div></article>`).join(''):'<p class="muted">لا يوجد أعضاء يملكون الرتب المحددة.</p>'}
async function load(){try{const response=await fetch('/api/public/members');if(!response.ok)throw Error();const data=await response.json();members=data.members||[];status.textContent=`تم تحميل ${members.length} عضو من Discord`;status.className='status ok';render()}catch(error){status.textContent='تعذر الاتصال بالبوت؛ تأكد من تشغيله وصلاحيات Server Members Intent';render()}}
search.addEventListener('input',render);document.querySelector('#year').textContent=new Date().getFullYear();load();
